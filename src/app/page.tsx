import { HomeContainer } from "@/containers";
import { DefaultLayout } from "@/layout";

export default function Home() {
  return (
    <DefaultLayout>
      <HomeContainer />
    </DefaultLayout>
  );
}
